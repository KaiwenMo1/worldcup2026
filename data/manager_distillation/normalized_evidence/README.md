# Normalized Manager Evidence

Reserved for versioned normalized evidence snapshots when provider or article-ingestion adapters are added. The current manager builder reads manually curated evidence directly from `raw_evidence/`.
